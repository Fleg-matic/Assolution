name: Frontend CI

on:
    push:
        paths:
            - "inscriptions-frontend/**"
            - ".github/workflows/frontend-ci.yml"
    pull_request:
        paths:
            - "inscriptions-frontend/**"

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3

            - name: Setup Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: "22"
                  cache: "npm"
                  cache-dependency-path: inscriptions-frontend/package-lock.json

            - name: Install dependencies
              run: |
                  cd inscriptions-frontend
                  npm ci

            - name: Run linter
              run: |
                  cd inscriptions-frontend
                  npm run lint

            - name: Run tests
              run: |
                  cd inscriptions-frontend
                  npm run test -- --watch=false --browsers=ChromeHeadless

            - name: Build application
              run: |
                  cd inscriptions-frontend
                  npm run build --prod

            - name: Upload build artifacts
              uses: actions/upload-artifact@v3
              with:
                  name: frontend-dist
                  path: inscriptions-frontend/dist/
